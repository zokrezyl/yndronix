org.gradle.jvmargs=-Xmx2g
# No AndroidX: we compile against API 28 with framework APIs only, to keep the
# toolchain (and the closure of build deps) minimal.
android.useAndroidX=false
android.nonTransitiveRClass=false
